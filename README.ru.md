# 🔐 ru-gost-crypto-lab

[![CI](https://github.com/ranas-mukminov/ru-gost-crypto-lab/workflows/CI/badge.svg)](https://github.com/ranas-mukminov/ru-gost-crypto-lab/actions)
[![License: Apache-2.0](https://img.shields.io/badge/License-Apache%202.0-blue.svg)](LICENSE)
[![Python 3.11+](https://img.shields.io/badge/python-3.11+-blue.svg)](https://www.python.org/downloads/)

🇷🇺 Русская версия | 🇬🇧 [English version](README.md)

**Лабораторный стенд для тестирования инфраструктуры с российской ГОСТ-криптографией (TLS, S/MIME, VPN).**

Автор: [Ranas Mukminov](https://github.com/ranas-mukminov) | Сайт: [run-as-daemon.ru](https://run-as-daemon.ru)

---

## Описание

**ru-gost-crypto-lab** — это комплексная лабораторная среда и набор инструментов для тестирования, проверки и бенчмаркинга компонентов инфраструктуры, использующих российские криптографические стандарты ГОСТ. Проект предоставляет шаблоны Infrastructure-as-Code (IaC), инструменты для проверки совместимости и скрипты для измерения производительности, помогая инженерам DevOps, системным администраторам и специалистам по безопасности оценить решения на основе ГОСТ перед развертыванием в продакшене.

Это **платформа для тестирования и обучения**, а не инструмент для продакшн-бэкапов. Для защищенных резервных копий с поддержкой ГОСТ используйте [gost-backup-vault](https://github.com/ranas-mukminov/gost-backup-vault).

Проект ориентирован на организации, которым необходимо соответствовать российским регуляторным требованиям (ФСТЭК, ФСБ) или которые хотят экспериментировать с ГОСТ-криптографией в веб-серверах, почтовых системах и VPN-конфигурациях.

## Основные возможности

- **Готовые IaC-сценарии**: шаблоны Docker Compose и Ansible для развертывания Web (Nginx/HAProxy с ГОСТ TLS), Mail (Postfix/Dovecot с S/MIME) и VPN-сервисов
- **Набор тестов совместимости**: автоматические проверки того, какие браузеры, почтовые клиенты и VPN-клиенты могут успешно подключаться к сервисам с ГОСТ
- **Бенчмаркинг производительности**: скрипты для измерения времени TLS-handshake, пропускной способности соединений и производительности VPN с ГОСТ-наборами шифров
- **Модульный Python-инструментарий**: чистые, типизированные модули Python 3.11+ для загрузки конфигураций, генерации сценариев и формирования отчетов
- **Интеграция с AI**: расширяемый интерфейс AI-провайдеров для автоматического анализа результатов тестов и рекомендаций
- **Многоязычные отчеты**: генерация подробных отчетов о совместимости и производительности на английском или русском языке
- **Готовность к CI/CD**: GitHub Actions для линтинга, сканирования безопасности (Bandit) и автоматического тестирования
- **Без проприетарных бинарников**: только открытые конфигурации и синтетические тестовые данные — пользователи устанавливают свои лицензированные СКЗИ/криптопровайдеры
- **Образовательная направленность**: понятная документация, примеры и тестовые сценарии для обучения и экспериментов

## Архитектура и компоненты

Проект состоит из нескольких уровней:

- **Пользовательский интерфейс**: CLI-инструменты для генерации сценариев и формирования отчетов (Markdown, JSON)
- **Уровень тестирования и анализа**: проверки совместимости (TLS, S/MIME, VPN) и бенчмарки производительности (handshake, throughput)
- **Уровень конфигурации**: парсер YAML-сценариев и валидация конфигурации
- **Уровень инфраструктуры (IaC)**: шаблоны Docker Compose и Ansible для стеков Web, Mail, VPN
- **Лабораторные сценарии**: YAML-определения для различных сценариев развертывания

**Ключевые компоненты:**
- Nginx/HAProxy с ГОСТ TLS-наборами шифров
- Postfix/Dovecot с поддержкой S/MIME (планируется)
- strongSwan/OpenVPN с ГОСТ-алгоритмами (планируется)
- Python-инструменты для проверки совместимости, бенчмарков производительности и генерации отчетов

## Требования

### Операционная система

- **Ubuntu 20.04+** или **Debian 11+** (основная поддержка)
- **RHEL 8+**, **Rocky Linux 8+**, **AlmaLinux 8+** (совместимы)
- **macOS** (для разработки; требуется Docker Desktop)

### Программные зависимости

- **Docker**: 20.10+ и Docker Compose 1.29+ (или Docker Compose V2)
- **Python**: 3.11 или выше
- **pip**: рекомендуется последняя версия
- **Git**: для клонирования репозитория

### Рекомендации по оборудованию

- **CPU**: 2+ ядра (4+ рекомендуется для тестирования производительности)
- **RAM**: минимум 4 ГБ, рекомендуется 8 ГБ
- **Диск**: 10 ГБ свободного места для Docker-образов и логов

### Сетевые требования

- **Доступ в Интернет**: для загрузки Docker-образов и установки пакетов Python
- **Открытые порты**: 443 (HTTPS), 25/587/993 (почта, при тестировании), 500/4500 (VPN, при тестировании)

### Опциональные зависимости

- **ГОСТ-криптопровайдер**: CryptoPro CSP, ViPNet или другое лицензированное СКЗИ (НЕ входит в репозиторий; пользователь должен установить самостоятельно)
- **Ansible**: 2.12+ (только если используете Ansible IaC-шаблоны)
- **openssl с движком ГОСТ**: для ручной генерации сертификатов и тестирования

> [!IMPORTANT]
> Этот репозиторий НЕ включает проприетарные бинарники СКЗИ. Вы должны получить и установить криптопровайдеры в соответствии с вашими лицензиями и местным законодательством.

## Быстрый старт

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/ranas-mukminov/ru-gost-crypto-lab.git
cd ru-gost-crypto-lab
```

### 2. Установите зависимости Python

```bash
pip install --upgrade pip
pip install .
```

Для разработки:

```bash
pip install .[dev]
```

### 3. Запустите лабораторную среду

```bash
docker-compose -f iac/docker-compose/docker-compose.web.yml up -d
```

Это запускает Nginx на порту 443 (HTTPS) и бэкенд-приложение на порту 8080.

### 4. Запустите тесты совместимости

```bash
python3 -m tools.compatibility.check_tls_clients --target localhost:443
```

### 5. Остановите лабораторию

```bash
docker-compose -f iac/docker-compose/docker-compose.web.yml down
```

## Установка

### Ubuntu / Debian

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER

# Установка Docker Compose
sudo apt install docker-compose -y

# Установка Python 3.11+
sudo apt install python3.11 python3-pip -y

# Клонирование и установка проекта
git clone https://github.com/ranas-mukminov/ru-gost-crypto-lab.git
cd ru-gost-crypto-lab
pip install .
```

### RHEL / Rocky / AlmaLinux

```bash
# Обновление системы
sudo dnf update -y

# Добавление репозитория Docker и установка
sudo dnf config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo
sudo dnf install docker-ce docker-ce-cli containerd.io -y
sudo systemctl start docker
sudo systemctl enable docker
sudo usermod -aG docker $USER

# Установка Docker Compose
sudo dnf install docker-compose-plugin -y

# Установка Python 3.11
sudo dnf install python3.11 python3.11-pip -y

# Клонирование и установка
git clone https://github.com/ranas-mukminov/ru-gost-crypto-lab.git
cd ru-gost-crypto-lab
python3.11 -m pip install .
```

## Настройка

### Пример Docker Compose

Расположен в `iac/docker-compose/docker-compose.web.yml`. Примонтируйте ваши ГОСТ-сертификаты:

```yaml
volumes:
  - /path/to/gost-cert.pem:/etc/nginx/ssl/cert.pem:ro
  - /path/to/gost-key.pem:/etc/nginx/ssl/key.pem:ro
```

### Определение сценария

Пример в `lab_scenarios/web_portal_gov/scenario.yaml`:

```yaml
name: web_portal_gov
type: web_tls
topology:
  services:
    - nginx_gost
    - backend_app
test_plan:
  compatibility: true
  performance: true
```

### Переменные окружения

Создайте файл `.env`:

```bash
COMPOSE_PROJECT_NAME=gost_lab
TEST_TARGET_HOST=localhost
TEST_TARGET_PORT=443
REPORT_LANGUAGE=ru  # или 'en'
```

## Использование

### Запуск/остановка сервисов

```bash
# Запуск
docker-compose -f iac/docker-compose/docker-compose.web.yml up -d

# Остановка
docker-compose -f iac/docker-compose/docker-compose.web.yml down

# Просмотр логов
docker-compose -f iac/docker-compose/docker-compose.web.yml logs -f
```

### Запуск тестов

```bash
# Совместимость TLS
python3 -m tools.compatibility.check_tls_clients --target <IP_СЕРВЕРА>:443

# Время TLS handshake
python3 -m tools.perf.bench_tls_handshake --target <IP_СЕРВЕРА>:443

# Пропускная способность TLS
python3 -m tools.perf.bench_tls_throughput --target <IP_СЕРВЕРА>:443
```

### Генерация отчетов

```bash
python3 -m tools.reports.report_builder --output отчет.md --language ru
```

## Обновление

```bash
cd ru-gost-crypto-lab
git pull origin main
pip install --upgrade .
docker-compose pull
docker-compose up -d
```

Проверьте [CHANGELOG.md](CHANGELOG.md) на предмет критических изменений.

## Устранение неполадок

### Частые проблемы

**Сервис не запускается:**
```bash
docker-compose logs <имя_сервиса>
```

**Порт уже используется:**
```bash
sudo lsof -i :443
sudo systemctl stop nginx
```

**Отказ в доступе (Docker):**
```bash
sudo usermod -aG docker $USER
# Выйдите и войдите снова
```

**Ошибки TLS handshake:**
```bash
openssl s_client -connect localhost:443
openssl engine gost
```

**Модуль не найден:**
```bash
pip install .
# Или для разработки
pip install -e .
```

## Замечания по безопасности

> [!CAUTION]
> Это **лабораторная среда**. Не выставляйте её напрямую в публичный Интернет без дополнительной защиты.

- **Смените пароли по умолчанию**, если есть дефолтные учетные данные
- **Ограничьте доступ** правилами firewall или через VPN
- **Не используйте продакшн-ключи** — все сертификаты и ключи должны быть только тестовыми
- **Обновляйте Docker-образы**: `docker-compose pull`
- **Используйте HTTPS/VPN для удаленного доступа**
- **Никаких реальных данных** в лабораторной среде

См. [LEGAL.md](LEGAL.md) для подробной правовой информации и требований соответствия.

## Структура проекта

```
ru-gost-crypto-lab/
├── .github/workflows/      # CI/CD пайплайны (lint, test, security scan)
├── iac/                    # Шаблоны Infrastructure-as-Code
│   ├── ansible/            # Ansible-роли
│   └── docker-compose/     # Файлы Docker Compose
├── lab_scenarios/          # YAML-определения сценариев
├── scripts/                # Вспомогательные скрипты (lint, test, security scan)
├── tests/                  # Набор тестов (pytest)
│   ├── integration/
│   └── unit/
├── tools/                  # Python-модули
│   ├── ai/                 # Интерфейс AI-провайдера
│   ├── compatibility/      # Проверки TLS, S/MIME, VPN
│   ├── perf/               # Бенчмарки производительности
│   └── reports/            # Генерация отчетов
├── CHANGELOG.md
├── CONTRIBUTING.md
├── LEGAL.md
├── LICENSE
├── README.md
├── README.ru.md
└── pyproject.toml
```

## Планы развития

### Текущий статус (v0.1.0)

- ✅ Базовая структура проекта
- ✅ Шаблон Docker Compose для Web TLS
- ✅ Python-модули для загрузки конфигураций и сценариев
- ✅ Stub-реализации для тестирования совместимости и производительности
- ✅ CI/CD с GitHub Actions
- ✅ Двуязычная документация (EN/RU)

### Планируется

- Дополнительные сценарии (Mail, VPN)
- Реальные проверки совместимости с фактическими SSL/TLS-соединениями
- Метрики производительности с Prometheus
- AI-аналитика и рекомендации
- Grafana-дашборды
- Ansible-плейбуки для развертывания на нескольких хостах
- Автоматизация генерации сертификатов

## Как внести вклад

Вклады приветствуются! Пожалуйста:

1. Форкните репозиторий
2. Создайте feature-ветку: `git checkout -b feature/my-feature`
3. Внесите изменения и закоммитьте: `git commit -m "Add feature"`
4. Запушьте в свой форк: `git push origin feature/my-feature`
5. Откройте Pull Request

### Требования

Все PR должны пройти:
- Линтинг: `./scripts/lint.sh`
- Тесты: `./scripts/dev_run_all_tests.sh`
- Сканирование безопасности: `./scripts/security_scan.sh`
- Codex Code Review: добавьте комментарий `@codex review`

См. [CONTRIBUTING.md](CONTRIBUTING.md) для деталей.

## Лицензия

Apache License 2.0. См. [LICENSE](LICENSE) для полного текста.

## Автор и коммерческая поддержка

**Автор:** [Ranas Mukminov](https://github.com/ranas-mukminov)

**Сайт:** [run-as-daemon.ru](https://run-as-daemon.ru)

### Коммерческие услуги

Для внедрения в продакшене, кастомных лабораторных сценариев и профессиональной поддержки:

- Развертывание и настройка лаборатории под вашу инфраструктуру
- Аудиты совместимости и подробные отчеты
- Анализ и оптимизация производительности
- Консалтинг по выбору и интеграции СКЗИ
- Обучение и воркшопы для DevOps/security команд
- Постоянная поддержка и сопровождение

Свяжитесь через [run-as-daemon.ru](https://run-as-daemon.ru) или [GitHub профиль](https://github.com/ranas-mukminov).

---

**Дисклеймер:** Этот проект предоставляется "как есть" без гарантий. Пользователи несут ответственность за соблюдение всех применимых законов и правил в области криптографии.
